{
  "name": "effect-doctor-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "audit": "turbo run //#audit:dupes:repo //#audit:health:repo",
    "audit:dependencies": "turbo run //#audit:dependencies:repo",
    "bench": "turbo run bench --filter=@ocarinalabs/effect-doctor",
    "build": "turbo run build",
    "catalog:check": "turbo run catalog:check --filter=@effect-doctor/core",
    "catalog:generate": "turbo run catalog:generate --filter=@effect-doctor/core",
    "check": "turbo run catalog:check typecheck test //#lint:repo //#test:action:repo",
    "doctor:self": "turbo run doctor:self --filter=@ocarinalabs/effect-doctor",
    "dev": "turbo run dev",
    "fix": "turbo run //#fix:repo",
    "lint": "turbo run //#lint:repo",
    "prose": "bun tooling/prose.ts",
    "prose:sync": "vale sync",
    "setup:effect": "turbo run setup:effect --filter=@effect-doctor/api",
    "test": "turbo run test",
    "test:action": "turbo run //#test:action:repo",
    "test:watch": "turbo run test:watch",
    "typecheck": "turbo run typecheck",
    "verify:package": "turbo run verify:package",
    "verify:release": "turbo run verify:release",
    "audit:dupes:repo": "fallow dupes --no-cache --fail-on-issues --summary --threads 4",
    "audit:health:repo": "fallow health --max-crap 1000 --no-cache --fail-on-issues --summary --threads 4",
    "audit:dependencies:repo": "bun audit --audit-level=high",
    "fix:repo": "ultracite fix",
    "lint:repo": "ultracite check",
    "test:action:repo": "node --test action/tests/*.test.mjs"
  },
  "devDependencies": {
    "@types/node": "26.4.0",
    "fallow": "3.20.0",
    "oxfmt": "0.65.0",
    "oxlint": "1.80.0",
    "oxlint-plugin-effect": "0.11.0",
    "slopsift": "0.11.0",
    "turbo": "2.10.12",
    "typescript": "7.0.2",
    "ultracite": "7.10.7",
    "vite-plus": "0.3.0",
    "vitest": "4.1.11"
  },
  "engines": {
    "node": ">=22.18.0"
  },
  "packageManager": "bun@1.4.0"
}
